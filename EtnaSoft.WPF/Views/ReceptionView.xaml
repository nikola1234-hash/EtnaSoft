<UserControl
    x:Class="EtnaSoft.WPF.Views.ReceptionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dxsch="http://schemas.devexpress.com/winfx/2008/xaml/scheduling"
    xmlns:local="clr-namespace:EtnaSoft.WPF.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <dxsch:SchedulerControl
            ActiveViewIndex="0"
            FirstDayOfWeek="Monday"
            GroupType="Resource"
            ShowDateNavigationPanel="True">
            <dxsch:SchedulerControl.DataSource>
                <dxsch:DataSource AppointmentsSource="{Binding Reservations}" ResourcesSource="{Binding Rooms}">
                    <dxsch:DataSource.ResourceMappings>
                        <dxsch:ResourceMappings Caption="RoomNumber" Id="Id" />
                    </dxsch:DataSource.ResourceMappings>
                </dxsch:DataSource>
            </dxsch:SchedulerControl.DataSource>


            <dxsch:TimelineView
                x:Name="timelineView1"
                ResourcesPerPage="10"
                SnapToCellsMode="Auto" />
        </dxsch:SchedulerControl>
    </Grid>
</UserControl>
